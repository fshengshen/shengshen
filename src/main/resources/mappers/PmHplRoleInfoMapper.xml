<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<!--
角色信息。操作相关SQL Mapper
@author fyn
@date 2016-08-02
@source Generated by MySQL2Java
-->

<!-- 此命名空间必须和对应Mapper.java文件的命名空间一致 -->
<mapper namespace="com.mb.memp.persistence.mapper.PmHplRoleInfoMapper">

    <!-- 角色信息。栏位列表 -->
    <resultMap id="rm" type="com.mb.memp.persistence.beans.PmHplRoleInfo">
        <result property="roleId" jdbcType="VARCHAR" column="role_id" />
        <result property="areaId" jdbcType="INTEGER" column="area_id" />
        <result property="sysId" jdbcType="VARCHAR" column="sys_id" />
        <result property="roleParentId" jdbcType="VARCHAR" column="role_parent_id" />
        <result property="roleIndex" jdbcType="VARCHAR" column="role_index" />
        <result property="roleName" jdbcType="VARCHAR" column="role_name" />
        <result property="roleDesc" jdbcType="VARCHAR" column="role_desc" />
        <result property="rolePermitLevel" jdbcType="VARCHAR" column="role_permit_level" />
        <result property="addBy" jdbcType="VARCHAR" column="add_by" />
        <result property="addName" jdbcType="VARCHAR" column="add_name" />
        <result property="addTime" jdbcType="VARCHAR" column="add_time" />
        <result property="lastEditBy" jdbcType="VARCHAR" column="last_edit_by" />
        <result property="lastEditName" jdbcType="VARCHAR" column="last_edit_name" />
        <result property="lastEditTime" jdbcType="VARCHAR" column="last_edit_time" />
        <result property="delFlag" jdbcType="VARCHAR" column="del_flag" />
    </resultMap>

    <!-- 获取角色信息。 -->
    <select id="getPmHplRoleInfo" resultMap="rm" parameterType="com.mb.memp.persistence.beans.PmHplRoleInfo">
        SELECT *
          FROM pm_hpl_role_info
         WHERE role_id = #{roleId}
    </select>

    <!-- 添加角色信息。 -->
    <insert id="addPmHplRoleInfo" parameterType="com.mb.memp.persistence.beans.PmHplRoleInfo">
        INSERT INTO pm_hpl_role_info(role_id,area_id,sys_id,role_parent_id,role_index,role_name,role_desc,role_permit_level,add_by,add_name,add_time,last_edit_by,last_edit_name,last_edit_time,del_flag)
        VALUES (#{roleId},#{areaId},#{sysId},#{roleParentId},#{roleIndex},#{roleName},#{roleDesc},#{rolePermitLevel},#{addBy},#{addName},#{addTime},#{lastEditBy},#{lastEditName},#{lastEditTime},#{delFlag})
    </insert>

    <!-- 修改角色信息。 -->
    <update id="editPmHplRoleInfo" parameterType="com.mb.memp.persistence.beans.PmHplRoleInfo">
        UPDATE pm_hpl_role_info
           SET area_id=#{areaId},
               sys_id=#{sysId},
               role_parent_id=#{roleParentId},
               role_index=#{roleIndex},
               role_name=#{roleName},
               role_desc=#{roleDesc},
               role_permit_level=#{rolePermitLevel},
               add_by=#{addBy},
               add_name=#{addName},
               add_time=#{addTime},
               last_edit_by=#{lastEditBy},
               last_edit_name=#{lastEditName},
               last_edit_time=#{lastEditTime},
               del_flag=#{delFlag}
         WHERE role_id = #{roleId}
    </update>

    <!-- 作废角色信息。 -->
    <update id="delPmHplRoleInfo" parameterType="com.mb.memp.persistence.beans.PmHplRoleInfo">
        UPDATE pm_hpl_role_info
            SET del_flag=1
         WHERE role_id = #{roleId}
    </update>

</mapper>
